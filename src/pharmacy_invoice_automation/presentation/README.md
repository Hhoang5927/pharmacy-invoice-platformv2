# Presentation Layer (Package 04)

The PySide6 desktop UI: main window, tabs, dialogs, workers, and
ViewModels. Only `presentation.viewmodels` may import `application.*`.

Implemented in **Prompt 07**.

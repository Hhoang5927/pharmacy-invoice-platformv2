# Shared

Framework-agnostic helpers (`Result` type, small utilities) usable from
any layer without pulling in any layer-specific dependency.

Implemented in **Prompt 04**, alongside the Domain layer.

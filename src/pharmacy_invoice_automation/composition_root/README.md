# Composition Root (Package 05)

The single place in the entire system where every concrete
Infrastructure adapter is bound to its `domain.ports` interface — the
one deliberate exception to the layer dependency rule.

Implemented in **Prompt 08**, once Packages 01–04 are complete.

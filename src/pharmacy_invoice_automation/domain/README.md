# Domain Layer (Package 01)

Entities, value objects, enums, exceptions, business rules, and ports.
Depends on nothing outside the Python standard library — see
`docs/architecture/Implementation_Specification.md` Section 5
(Dependency Rules) for the enforced import boundary.

Implemented in **Prompt 04**.

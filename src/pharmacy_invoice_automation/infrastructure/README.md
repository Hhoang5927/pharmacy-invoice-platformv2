# Infrastructure Layer (Package 03)

Concrete adapters implementing `domain.ports`: logging, configuration
and secrets, SQLite persistence, the Gemini OCR adapter, and the
Playwright automation adapter (including the Selector Registry,
session storage, and Long Châu price lookup).

Implemented in **Prompt 05**.
